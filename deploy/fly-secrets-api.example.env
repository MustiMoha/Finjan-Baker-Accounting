# Example Fly secrets — copy values, then:
# fly secrets set -c deploy/fly-api.toml --env-file deploy/fly-secrets-api.env
# (create the .env file locally; never commit real secrets)

SUPABASE_URL=https://cafomnrnrniqigzvbhho.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
AUTH_WEB_URL=https://your-app.vercel.app
STREAMLIT_URL=https://ali-al-baker-streamlit.fly.dev
SERVE_AUTH_UI=false
MASTER_WORKBOOK_BUCKET=accounting-master
DOCUMENTS_BUCKET=accounting-documents
