streamlit>=1.33.0
extra-streamlit-components>=0.1.71
# 2.8.x rejects publishable keys (sb_publishable_...) with client-side JWT regex. Use 2.22.4+
# Cap <2.26: newer storage3 pulls pyiceberg (often needs MSVC on Windows).
supabase>=2.22.4,<2.26
fastapi>=0.116.0
# Streamlit 1.57+ imports DEFAULT_EXCLUDED_CONTENT_TYPES from starlette.middleware.gzip (added in 0.46+).
# FastAPI <0.116 caps starlette at <0.42, which breaks Streamlit startup.
starlette>=0.46.0,<0.49.0
uvicorn[standard]>=0.32.0
openpyxl>=3.1.0
xlrd>=2.0.1
pandas>=2.0.0
pdfplumber>=0.11.0
argostranslate>=1.9.0
# JPG/PNG invoice OCR (also install Tesseract — see invoice_extract / .env.example)
Pillow>=10.0.0
pytesseract>=0.3.10
